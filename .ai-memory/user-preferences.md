# User Preferences

- **Communication style:** Terse. Skip preamble. Always list remaining tasks at the end of multi-step projects.
- **On `next`:** if tasks appear finished, recall prior chat to find leftover work and suggest it.
- **Default to subtle for pencil strength** — explicitly stated preference.
- **Cross-platform constraint:** AI runs in a Linux sandbox; Windows-only operations (rebuilding `.exe`, capturing real screenshots/GIFs, registering context menus, tagging releases) MUST be delegated to the user with clear instructions.
- **Version bumps:** when the user says "bump the version" without a target, ask or pick a sensible semver bump; recently they picked `1.1.0` to reset to a clean major.
