# Enum Naming Quick Reference

## Rules
1. **PascalCase:** Enum names and members must be PascalCase.
2. **String-Backed:** Use string-backed enums for all serialized/persisted values.
3. **Exhaustive Matching:** Always handle all enum cases explicitly; do not rely on silent wildcard fallbacks.
