# Multi-line Formatting

## Rules
1. **Parameter Lists:** If a function has more than 2-3 parameters, or exceeds 80 characters, format each parameter on its own line.
2. **Trailing Commas:** Use trailing commas in multiline argument lists and collections where supported.
3. **Parameter Objects:** When passing configuration or options, wrap them in a parameter struct/dict.
