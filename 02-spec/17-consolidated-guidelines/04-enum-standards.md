# Enum Standards

## Standard
Enums represent fixed sets of discrete values:
- Use PascalCase for enum names and members.
- Ensure exhaustiveness in pattern matching or switch/if chains.
- Provide string representations for logs and serialization.
